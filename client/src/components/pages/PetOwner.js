import React from 'react';
import { Wrapper, Title } from '../../GlobalStyles/page-styles';

const PetOwner = () => {
 return (
  <Wrapper>
    <Title>Client (pet owner)</Title>
  </Wrapper>
 );
};

export default PetOwner;